# Installation

## Requirements

- Windows or Linux with python == 3.13.12 (I didn't try another version)

```
pip install -r docs/requirements.txt
```

I didn't try to see if the [docker setup](https://github.com/Erdfin/fast-reid/blob/master/docker/README.md) still works because I'm using this a module for the inference part. So there be dragons.
